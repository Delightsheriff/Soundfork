public enum AppIdentity {
    /// Also the prefix of our own processes, which are never listed as routable apps, and the logging subsystem.
    public static let bundleID = "com.delightsheriff.Soundfork"
}
